export enum BUTTON_TYPES {
    BUTTON = "button",
    SUBMIT = "submit",
    RESET = "reset",
}

export enum BUTTON_VARIANT {
    PRIMARY = "primary",
    SECONDARY = "secondary",
    DELETE = "delete",
}
